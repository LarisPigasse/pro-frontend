// Barrel file per middleware
export { default as persistenceMiddleware, storageUtils } from "./persistenceMiddleware";

// Re-export types if needed
export type { default as PersistenceMiddlewareType } from "./persistenceMiddleware";
